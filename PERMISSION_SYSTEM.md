# ระบบจัดการสิทธิ์ (Permission System)

## ภาพรวม

ระบบนี้ช่วยจำกัดสิทธิ์การเข้าถึงหน้าต่างๆ ในระบบ โดยมี 3 role หลัก:

1. **Super Admin** - เข้าถึงได้ทุกหน้า + จัดการสิทธิ์ผู้ใช้อื่น
2. **Admin** - เข้าถึงหน้าที่ได้รับอนุญาต
3. **User** - เข้าถึงเฉพาะหน้าผู้ใช้ทั่วไป

## การตั้งค่า

### 1. ตั้งค่า Environment Variable

เพิ่มใน `.env.local`:

```env
SUPERADMIN_SECRET=your-secret-key-here
```

### 2. ตั้งค่า Super Admin คนแรก

1. ไปที่ `/admin/superadmin/setup`
2. กรอก `SUPERADMIN_SECRET` ที่ตั้งไว้
3. เลือกบัญชีที่ต้องการตั้งเป็น Super Admin
4. กดปุ่มตั้งค่า

### 3. จัดการสิทธิ์ผู้ใช้

1. Super Admin ไปที่ `/admin/superadmin`
2. เลือกผู้ใช้ที่ต้องการจัดการ
3. เลือก Role และสิทธิ์หน้าที่ต้องการให้เข้าถึง
4. กดบันทึก

## โครงสร้างไฟล์

```
lib/
  permissions.ts        # กำหนดค่าสิทธิ์และหน้าต่างๆ

pages/api/permissions/
  get-user.js          # ดึงสิทธิ์ผู้ใช้
  update-user.js       # อัปเดตสิทธิ์ผู้ใช้
  get-all-users.js     # ดึงผู้ใช้ทั้งหมดพร้อมสิทธิ์
  make-superadmin.js   # ตั้งค่า superadmin

pages/admin/superadmin/
  index.jsx            # หน้าจัดการผู้ใช้และสิทธิ์
  setup.jsx            # หน้าตั้งค่า superadmin ครั้งแรก

components/
  PermissionGuard.tsx  # Component สำหรับตรวจสอบสิทธิ์
```

## การเพิ่มหน้าใหม่

1. เปิดไฟล์ `lib/permissions.ts`
2. เพิ่มหน้าใหม่ใน `ALL_PAGES`:

```typescript
{
  path: '/admin/new-page',
  label: 'หน้าใหม่',
  icon: '🆕',
  description: 'คำอธิบายหน้า',
  category: 'management'
}
```

3. เพิ่มใน `DEFAULT_PERMISSIONS` ถ้าต้องการให้ role ใดเข้าถึงได้โดย default

## การตรวจสอบสิทธิ์ใน Page

ระบบจะตรวจสอบสิทธิ์อัตโนมัติใน `_app.tsx` สำหรับหน้าที่เริ่มต้นด้วย `/admin` หรือ `/user`

ถ้าต้องการตรวจสอบเพิ่มเติมใน component:

```tsx
import { usePermissions } from '@/components/PermissionGuard';

function MyComponent() {
  const { checkPermission, isSuperAdmin, isAdmin } = usePermissions();
  
  if (!checkPermission('/admin/some-page')) {
    return <div>ไม่มีสิทธิ์</div>;
  }
  
  return <div>Content</div>;
}
```

## หมายเหตุ

- สิทธิ์จะถูกเก็บใน Clerk private metadata เป็น fallback หาก Backend API ไม่พร้อมใช้งาน
- Super Admin สามารถตั้งค่า Super Admin คนอื่นได้
- เมื่อเปลี่ยน role จะ auto-reset สิทธิ์เป็น default ของ role นั้น

